; Numeric functions

(define (dec n)
  (- n 1))

(define (inc n)
  (+ n 1))

(define (pos? n)
  (> n 0))

(define (zero? n)
  (= n 0))

(define (neg? n)
  (< n 0))

(define (even? n)
  (zero? (mod n 2)))

(define (odd? n)
  (not (even? n)))

