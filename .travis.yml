language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
# command to install dependencies
install:
#  - "pip install ."
  - "pip install -r src/python/requirements.txt"
  - "pip install coverage"
# command to run tests
script: nosetests --with-coverage --cover-package yalix -w src/python
